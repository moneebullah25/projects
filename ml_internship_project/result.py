text = [Document(page_content='This text splitter is the recommended one for generic text. It is parameterized by a list of characters. It tries to split on them in order until the chunks are small enough. The default list of separators is ["', metadata={'start_index': 1}), 
Document(page_content='", "\n", " ", ""]. This has the effect of trying to keep all paragraphs (and then sentences, and then words) together as long as possible, as those would generically seem to be the strongest semantically related pieces of text.', metadata={'start_index': 214}), 
Document(page_content='How the text is split: by list of characters\nHow the chunk size is measured: by number of characters', metadata={'start_index': 442}), 
Document(page_content="Important parameters to know here are chunkSize and chunkOverlap. chunkSize controls the max size (in terms of number of characters) of the final documents. chunkOverlap specifies how much overlap there should be between chunks. This is often helpful to make sure that the text isn't split weirdly.", metadata={'start_index': 543}), 
Document(page_content="should be between chunks. This is often helpful to make sure that the text isn't split weirdly. In the example below we set these values to be small (for illustration purposes), but in practice they default to 1000 and 200 respectively.", metadata={'start_index': 746})]